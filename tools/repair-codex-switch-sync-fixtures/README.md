# repair-codex-switch-sync fixtures

Synthetic fixtures for `repair-codex-switch-sync-core.js`.

Rules:
- Do not copy real `auth.json`.
- Do not copy real `cc-switch.db`.
- Do not include real API keys, OAuth tokens, refresh tokens, cookies, or sessions.
- Use placeholder values such as `<TOKEN>` and `<API_KEY>`.
